services:
  kafka-ui:
    image: provectuslabs/kafka-ui:latest
    container_name: kafka-ui
    ports:
      - "8989:8080"
    environment:
      KAFKA_CLUSTERS_0_NAME: orbstack
      KAFKA_CLUSTERS_0_BOOTSTRAPSERVERS: host.docker.internal:9092
